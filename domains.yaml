# Host suffixes logged as [local_router] by local_router.py.
# Matching is literal: a leading dot is safest for subdomains; without one,
# "acme.dev" also matches names such as "notacme.dev".
domains:
  - ".example.com"
